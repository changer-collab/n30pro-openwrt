# 磊科 N30 Pro · OpenWrt 云编译包（含校园网插件）

这个文件夹是准备好在 **GitHub Actions** 上云编译用的整套配置，目标：为你的 **磊科 N30 Pro**（MT7981B / aarch64_cortex-a53）编译一个 **OpenWrt 系统，并已内置校园网认证插件**（mentohust + ua2f）。

**这套流程全程不碰你的路由器、不断网。** 编译在 GitHub 云端完成，你只需要把文件放到 GitHub 上点运行。

---

## 文件说明

| 文件 | 作用 |
|---|---|
| `.github/workflows/build-openwrt.yml` | GitHub Actions 工作流：自动拉源码、装插件、编译、产出固件 |
| `n30pro.config` | 编译配置：指定 N30 Pro 机型 + 勾选 mentohust/ua2f 等插件 |
| `package/n30pro/` | 插件占位目录（实际插件源码在 workflow 里直接 clone） |

---

## 你需要做的操作（有 GitHub 账号即可，免费）

1. 在 GitHub 新建一个**公开仓库**（名字随意，如 `n30pro-openwrt`）。
2. 把本文件夹里的内容**上传/推送到这个仓库**（至少包含 `.github/`、`n30pro.config`）。
3. 打开仓库页的 **Actions** 标签 → 选择 **Build-N30Pro-OpenWrt** → 点 **Run workflow**。
4. 编译约 **1~2 小时**。完成后在该次运行的 **Artifacts** 里下载 `n30pro-openwrt-firmware`。
5. 解压后，找到这样的文件：`*netcore_n30pro-squashfs-sysupgrade.itb` —— 这个就是要刷进路由器的固件。

> 如果不想用 Git 命令，也可以在 GitHub 页面点 `Add file → Upload files`，直接把 `n30pro.config` 和 `.github` 整个上传。

---

## 如果编译报错怎么办（常见）

- **克隆插件失败**：workflow 里每个 `git clone` 都带了 `|| echo "... failed"`，即使某个插件拉不下来，主线还是会继续编译出**不带该插件**的基础固件。所以即使部分失败，你仍能得到一个能用的 OpenWrt，插件可以之后再手动 `opkg install`。
- **机型名不匹配**：`n30pro.config` 里的机型 `netcore_n30pro` 对应官方源码主线已合入的设备名。如果报机型不存在，说明官方源码树里该设备的 profile 名有出入，把报错信息发给我，我帮你改。

---

## 刷入固件（等编译完成、我再带你做）

这一步会**涉及操作路由器/可能短暂断网**，到时候我会**提前、明确地**告诉你怎么按键、怎么连网线、怎么进 uboot Web 上传固件。你现在**不用做任何路由器操作**。

---

## 提醒

- 全程不要动你已经写好的 uboot 和备份，它们都安全。
- 固件编译好后，若带 `*factory.bin` 和 `*sysupgrade.itb` 两种，N30 Pro 用 **sysupgrade** 那个在 uboot 里刷。
